# Background lists for Over Representation Analysis (ORA)

`background_list()` returns a list of background gene sets for Over
Representation Analysis (ORA).

## Usage

``` r
background_lists()
```

## Value

A list of background gene sets for Over Representation Analysis (ORA).
