# Select background list for Over Representation Analysis (ORA)

`select_background()` is used to select a background list for Over
Representation Analysis (ORA).

## Usage

``` r
select_background(background)
```

## Arguments

- background:

  A character string specifying the background list to be used.

## Value

A character vector of gene symbols.
